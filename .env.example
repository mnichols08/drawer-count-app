# Copy this file to .env and adjust as needed
# NOTE: .env is ignored by git. Do not commit secrets.

# ------------------------------------------
# MongoDB (local development defaults)
# ------------------------------------------
# By default, use a local MongoDB instance.
# If you instead use Atlas/remote, comment these and use the SRV URI in the
# "Remote/Atlas example" section below.
MONGODB_URI=mongodb://127.0.0.1:27017/?directConnection=true
MONGODB_DB=drawer-count-app
MONGODB_TLS=false
MONGODB_SERVER_SELECTION_TIMEOUT_MS=3000

# ------------------------------------------
# Frontend runtime config (optional)
# ------------------------------------------
# When running locally, the server automatically exposes '/api' to the frontend.
# Leave API_BASE empty to avoid proxying loops if MONGODB_URI is not set.
# API_BASE=

# ------------------------------------------
# Remote/Atlas example (uncomment and edit if using remote DB)
# ------------------------------------------
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/
# MONGODB_DB=drawer-count-app
# MONGODB_TLS=true

# ------------------------------------------
# Server
# ------------------------------------------
# Port the local Express server listens on
PORT=8080
