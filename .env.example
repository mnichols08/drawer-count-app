# Copy this file to .env and fill in the blanks as needed
# NOTE: .env is ignored by git. Do not commit secrets.

# Server
PORT=8080

# MongoDB (set these to enable server API)
# For MongoDB Atlas, prefer the SRV URI form.
# Example:
# MONGODB_URI="mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/drawercount?retryWrites=true&w=majority"
MONGODB_URI=
MONGODB_DB=drawer-count

# TLS options
# Most Atlas setups only need MONGODB_TLS=true and do NOT require any CA variables.
MONGODB_TLS=true
# Uncomment ONLY for diagnostics (not for production)
# MONGODB_TLS_INSECURE=true

# If your provider requires a custom CA (not typical for Atlas), use ONE of the following:
# MONGODB_TLS_CA_FILE=/absolute/path/to/ca.pem
# MONGODB_TLS_CA_PEM="-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----\n"
# MONGODB_TLS_CA_BASE64=BASE64_ENCODED_PEM

# Frontend runtime config (optional)
# Set when the API is on a different origin
# API_BASE=https://your-service.onrender.com/api
